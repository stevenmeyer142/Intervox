package com.brazedblue.intervox.data;import java.util.*;public class SortedList extends Vector{		protected static int k1LessThan2 = -1;	protected static int k1Equals2 = 0;	protected static int k1GreaterThan2 = 1;		protected boolean fAddDuplicates = false;	    public SortedList() 	{    }		public void Insert(Object object)	{		int index = GetIndex(object);				if (fAddDuplicates || index < 0)		{			if (index < 0)			{				index = -index - 1;			}			insertElementAt(object, index);		}	}		protected int Compare(Object obj1, Object obj2)	{		return k1GreaterThan2;	}		public int GetIndex(Object obj1)	{		int index = -1;		int compareResult = k1LessThan2;			if (size() == 0)			index = 0;		else		{			int low = 0; 			int high = size() - 1;			int currentIndex;			do			{				currentIndex = (low + high) / 2;								Object obj2 = elementAt(currentIndex);				compareResult = Compare(obj1, obj2);					if (compareResult <= k1LessThan2)					high = currentIndex - 1;				else					low = currentIndex + 1;				}			while (!((compareResult == k1Equals2) || (low > high)));				if (compareResult >= k1GreaterThan2)				currentIndex++;				// keep index in range 			index = ((currentIndex < 0) || (currentIndex > size())) ? 0 : currentIndex;		}		if (compareResult != k1Equals2)		{			index = -index - 1;		}				return index;	}	}