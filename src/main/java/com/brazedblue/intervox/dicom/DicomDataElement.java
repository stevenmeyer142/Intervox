package com.brazedblue.intervox.dicom;import java.io.IOException;import com.brazedblue.intervox.util.Debug;class DicomDataElement{	DicomElementHeader 	fHeader = new DicomElementHeader();	Object				fValue = null; 	private boolean				fIgnored = false;	static private final String kSequenceDelimitorTagStr = "FFFE,E0DD";			static public boolean gDebugSpitOutContents = false && Debug.fgDebugging;			DicomDataElement()	{		}		boolean Ignored() 	{		return fIgnored;	}		public String toString()	{		String result = /*super.toString() + "\r" + */ fHeader.toString() + "\r";		                result += "\rIgnored " + fIgnored;				if (fValue == null)		{			result += "Null value";		}		else if (fValue instanceof byte[])		{			result += new String((byte[])fValue);		}		else		{			result += fValue;		}                		return result;	}		// reads data in from a stream		// @return bytes read	public int Read(DicomDataSetInputStream stream, DicomElementFilter filter) throws IOException	{		long initialCount = stream.GetPos();			fHeader.Read(stream);				if (fHeader.GetTagString().contentEquals(DicomImageDataSet.kPixelDataTag))		{			System.out.println("Found image");		}		if ((filter.CanIgnore(fHeader, null) && fHeader.GetLength() > 0))		{			stream.skip(fHeader.GetLength());			fIgnored = true;		}		else		{			fValue = fHeader.ReadValue(stream, filter);		}				return (int)(stream.GetPos() - initialCount);	}		boolean IsSequenceDelimitor()	{		return kSequenceDelimitorTagStr.equals(fHeader.GetTagString());	}		void DebugDump()	{		if (Debug.fgDebugging)		{			Debug.DebugFilePrintln(toString());		}	}		String GetTagString()	{		return fHeader.GetTagString();	}		Object GetValue()	{		return fValue;	}		DicomElementHeader GetHeader()	{		return fHeader;	}}