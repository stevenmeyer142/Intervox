package com.brazedblue.intervox.display;import javax.swing.*;import java.awt.event.*;public class ImagesFrame extends JInternalFrame /* implements anInterface */ {      public ImagesFrame(String title, boolean resizable, boolean closable) 	{		super(title, resizable, closable);                setVisible(true);    }		protected JRootPane createRootPane()	{		return new ImagesRootPane();	}		protected void finalize() throws Throwable	{		super.finalize();	}  }