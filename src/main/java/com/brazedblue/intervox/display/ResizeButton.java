package com.brazedblue.intervox.display;import javax.swing.*;import java.awt.*;public class ResizeButton extends JComponent /* implements ComponentListener */ {		ResizeListener fResizeListener;		public ResizeButton(Container boundingContainer, Component resizingComponent) 	{		fResizeListener = new ResizeListener(boundingContainer, resizingComponent, this);				setAlignmentX(RIGHT_ALIGNMENT);		setAlignmentY(BOTTOM_ALIGNMENT);				Dimension size = new Dimension(16, 16);		setPreferredSize(size);		setMaximumSize(size);		setMinimumSize(size);		setSize(size);	}/*	public void componentResized(ComponentEvent e)	{		Component source = e.getComponent();		if (source != null && source == getParent())		{			Dimension preferredSize = getPreferredSize();			Dimension componentSize = source.getSize();			move(componentSize.width - preferredSize.width, componentSize.height - preferredSize.height);		}		else		{			NeuroSynchUtil.ErrorMessage("Component was not parent in ResizeButton.componentResized", false);		}	}		public void componentMoved(ComponentEvent e)	{		// do nothing	}		public void componentShown(ComponentEvent e)	{		// do nothing	}		public void componentHidden(ComponentEvent e)	{		// do nothing	}*/	protected void paintComponent(Graphics g) 	{		Dimension size = this.getSize();		g.draw3DRect(0, 0, size.width - 1, size.height - 1, true);    }}